# 🛠️ 담당별 기능 구현 체크리스트 (Task List)

## 🤝 전원 공통 및 프로젝트 설정 (Common & Env Setup)
- [ ] **통합 빌드 환경(Makefile) 구축 및 문서화 (도큐먼트 점수 20점 방어)**
  - [x] 컴파일 시 필수 링킹 옵션(`-pthread`, 한글 및 UI 깨짐 방지를 위한 `-lncursesw`)이 포함된 전용 `Makefile` 구축
  - [ ] 프로젝트 빌드 방법, 실행 옵션, 주요 기능이 완벽히 정리된 `README.md` 작성
- [ ] **패킷 프로토콜 엔디안(Endianness) 변환 규약 정립**
  - [x] `protocol.h` 헤더 작성 시 다중 클라이언트의 아키텍처(CPU) 호환성을 위해 `int` 등 4바이트 이상 데이터에 대해 송신 전 `htonl()`, 수신 후 `ntohl()` 강제 적용 보장
  - [x] **[B 요청 처리 완료]** `protocol.c`의 `swap_packet_endian` 및 `enforce_null_termination`에 `PKT_RES_LOGIN_FAIL` case 추가 완료
- [ ] **메모리 누수(Memory Leak) 점검 아키텍처**
  - [ ] 프로젝트 마무리(6주차) 단계 시, 3인 모두 `Valgrind`를 구동하여 소켓이나 연결 리스트 상에서 `malloc` 후 `free()`되지 않은 메모리 찌꺼기가 없는지 교차 검증

## 📡 User 1: 서버 네트워크 및 이벤트 스레드 담당 (Network & Background)
- [x] **소켓 통신 뼈대 및 구조체 설계**
  - [x] `socket()`, `bind()`, `listen()`, `accept()` 기반 포트 개방
  - [x] Thread-per-client 모델 `pthread_create()` 연동
  - [x] 로그인, 채팅, 찌라시, 매매 요청 등 패킷(`Packet`) 프로토콜 구조 구조화
  - [x] **[이중 접속 차단 및 세션 복구]** 신규 접속 시 전역 `active_keys`로 중복 로그인을 차단하고, `recv()<=0` 소켓 오류 감지로 끊긴 세션의 슬롯·Key를 자동 정리 (능동적 Ping/Pong 하트비트는 미구현 — [논의 1]에서 현 수준으로 충분 합의)
  - [x] **[망실 방어]** 클라이언트 강제 종료 시 `recv() <= 0` 예외를 Catch하여 해당 Socket 안전하게 `close()`
  - [x] 퇴장 유저를 '현재 접속자 배열'에서 삭제할 때 데이터 충돌을 막기 위한 전역 상태 `Mutex Lock` 적용
  - [x] **[늦은 접속자 동기화]** 로그인 성공(`PKT_RES_LOGIN_OK`) 직후, 현재 서버 메모리에 존재하는 모든 시장 매물과 NPC 의뢰를 해당 신규 클라이언트에게만 연속으로 단일 전송(Unicast)하여 화면 동기화
- [ ] **좀비 프로세스 방어 파이프라인 (Watchdog)**
  - [ ] 부모 `monitor` 프로세스에서 `fork()` 호출 후 `exec()` (ex: `execv`) 시스템 콜을 통해 자식 서버 교체 실행
  - [ ] 자식 프로세스(서버) 붕괴 시 `waitpid()` 로 백그라운드 추적 및 재가동 리스크 차단
- [ ] **비동기 자동화 이벤트 스레드 (Event Engine)**
  - [x] `sleep()` 타임 루프를 맴도는 백그라운드 이벤트 전이 전담 스레드 파생
  - [x] 무작위 속성(태그)을 가진 "시장 매물" 1개씩 스폰 시 `market_spawn(&doc)` + `sandbox_master_write(&doc)` 호출 및 전체 브로드캐스트 (B가 함수 제공 완료)
  - [x] "NPC 의뢰" 스폰 시 `npc_spawn(&npc)` 호출 및 브로드캐스트 (B가 함수 제공 완료)
  - [ ] **[사보타주 단일 전송]** 역할 B(DB)에서 `/rumor` 수수료 차감이 성공하면, 접속 중인 유저 배열을 순회해 `target_key`를 가진 대상 소켓을 찾아 즉시 글리치 패킷(`PKT_EVT_RUMOR_GLITCH`) 단일 전송(Unicast)
  - [ ] 간헐적으로 특정 태그 가치를 0으로 폭락시켜 3~5분간 NPC 스폰 및 상점 판매를 차단하는 "Public Leak(동결)" 뉴스 브로드캐스트 — `market_freeze_tags()` 호출 (B가 함수 제공 완료)
  - [ ] 동결 쿨다운 시간 종료 시, 해당 기밀의 가치를 다시 1.0배로 복구(재상장)시켜 기다린 유저에게 보상하는 기능 추가 — `market_recover_tags()` 호출 (B가 함수 제공 완료)
  - [x] 오래된 NPC 보드 의뢰 소멸(Aging/Despawn) 수거 루프 구축 (`npc_despawn_aged` + `event_age_npcs`, 수명 180초 초과 시 만료·브로드캐스트 — 라이브 검증 완료. ※원래 A 담당이나 B가 선구현)
  - [ ] 경찰 레이드 🚨 경고 패킷 파생 및 방송
  - [ ] **[게임 오버 브로드캐스트]** 역할 B(DB)로부터 탈출 승리자 발생 트리거를 전달받아 전역 상환 목표액을 실시간 인상 브로드캐스트(+2,000원, 최대 14,000원)하고, 누적 3명 도달 시 **경찰 레이드 습격 및 1분 서버 데이터 소각 리셋** 패킷 브로드캐스트 전송

## 🔒 User 2: 백엔드 파일 입출력 및 동기화 담당 (System, FS & DB) [최상위 난이도]
- [ ] **유저 DB 및 레코드 락 (File I/O Persistence)**
  - [x] 접속 유저에게 자본금 할당 및 `users.dat` 파일 추적 (초기자본 1000원, `userdb_find_or_create` 구현)
  - [x] `open()`, `lseek()`, `write()` 를 통한 자산 갱신 직접 제어 체계
  - [ ] 타 스레드가 수정 중일 때 보호하는 `fcntl()` 유저 자산 레코드 락 구현 (현재 `F_SETLK` 미적용 — `open/lseek/write`만 사용 중)
  - [x] **[논리적 계정 소각]** 파산(자금 0원 **AND 인벤토리 비어있음**) 시, 물리적 파일 삭제 대신 해당 유저의 `users.dat` 내 자산 수치를 특수 값(-1)으로 덮어씌워 재접속 영구 차단 (`userdb_burn_at`, `userdb_is_burned` 구현)
  - [ ] **[승리 트리거 및 자금 회수]** 매각 등으로 자산 갱신 시 **현재 유동적인 목표 상환액** 도달 여부를 실시간 검사하고, `/payoff` 성공 시 자금 회수 및 역할 A에게 승리자 발생 알림(목표 상환액 인상) 및 누적 3명 시 서버 리셋 트리거 전송
- [x] **인벤토리 샌드박스와 파일 시스템 디렉토리 연산 (File System)**
  - [x] 클라이언트 최초 유입 시 `mkdir()` 로 `./data/users/[user_id]/` 하위에 고유 개인 샌드박스 생성
  - [x] 마스터 상점(`./data/master/`) ↔ 유저 샌드박스 간 파일 이동 시 EXDEV 에러를 피하도록 물리적 경로 고정
  - [x] 매입/매각 시 `rename()` 호출하여 파일 객체를 원자적으로 은닉 및 `chmod()` 차단
  - [x] `/dispose` (강제 폐기) 명령 구동 시 `unlink()` 로만 파일 파쇄 (어떠한 수익 환급이나 보상도 없이 0원 처리)
  - [x] **[샌드박스 순회]** `/inventory` 요청 시, `opendir()`과 `readdir()`를 사용해 해당 유저의 경로 파일들을 순회하여 패킷 전송 (단, `.` 및 `..` 디렉토리 파싱 예외 처리 필수)
  - [x] **[용량 제한 로직]** 유저 인벤토리(샌드박스)의 파일 최대 개수를 강제(`MAX_INVEN_SIZE`)하고 초과 구매를 막는 검증 로직 구현
- [x] **이중 동시성 방어 및 교착상태(Deadlock) 원천 차단 알고리즘**
  - [x] **[조합 검증 로직]** `/sell` 요청 시, 제출한 속성 태그들의 합집합이 NPC 요구 조합을 완전히 포함(부분집합 관계)하는지 판별 및 초과 소모 기회비용 로직 구현
  - [x] **[동결 태그 차단]** `/sell` 시 해당 기밀이 공중파 유출로 '동결'된 상태인지 검증하여 매각을 단호히 거절하는 방어 로직 추가 (freeze mask 구현)
  - [x] **[사보타주 비용 차감]** `/rumor` 요청 시 유저 DB에서 막대한 수수료를 차감하고(수수료 500원), 돈이 충분할 경우 역할 A에게 브로드캐스트 요청 발송
  - [x] 상점 배열 접근 및 NPC 매각 시 중복 탈취 방지용 `pthread_mutex_lock` 적용
  - [x] **[핵심]** 파일 번호(ID)가 2개 이상일 경우 반드시 ID 번호 오름차순으로 Lock을 잠구어 Circular Wait 현상 봉쇄 (`market_doc_lock_many` — 평가 어필 포인트)

## 🖥️ User 3: 클라이언트 UI 및 시그널 하이재킹 담당 (UI/UX & Signals)
- [x] **클라이언트 스레드 투트랙 분리 및 수동 버퍼 구현**
  - [x] 클라이언트 실행 즉시 `pthread_create()` 호출로 **2-Thread Blocking 분리 구조 설계**
    - [x] `스레드 B (수신)` : `recv()` 로 서버 패킷 무한 대기 (기본 구조 존재, printf 수준)
    - [x] `스레드 A (메인)` : `wgetch()` 로 입력 프롬프트 블로킹 무한 대기 및 렌더링
  - [x] `cbreak()`, `noecho()`, `timeout(0)` 환경에서의 비동기 커스텀 `char buffer[]` 제한 입력 구현
- [ ] **Ncurses 6분할 윈도우 인터페이스 (UI Engine)**
  - [x] Ncurses 환경에서 한글 깨짐을 방지하기 위해 `initscr()` 호출 직전 반드시 `<locale.h>`의 `setlocale(LC_ALL, "");` 호출
  - [x] `initscr()` 등 호출 직후 `getmaxyx()` 연산으로 최소 해상도(100x30) 미달 시 프로그램 강제 종료 및 에러 문구 렌더링 (Fail-Safe 적용)
  - [x] 배율(Float) 할당의 정수 버림 오차 방지를 위해 최종단 윈도우는 잔여 길이(`COLS` 혹은 `LINES`에서 누적폭을 뺀 값)로 연산 할당
  - [x] `wgetch()` 대기 루프 내부에서 창 크기 조절 시스템 인터럽트를 잡아내는 `KEY_RESIZE` 분기문으로 `delwin()` 및 동적 `Redraw`
  - [x] **[창 분할 코딩]** 커서 스크롤 오작동 방지를 위해 화면 최하단부를 `출력용 채팅/로그 창(scrollok 활성)`과 `단 1줄 크기의 순수 입력용 윈도우(scrollok 비활성)`로 분리 선언
  - [x] **[Buffer Overflow 차단]** 순수 입력창의 가용 너비(전체폭에서 퀵헬프 길이를 제외한 범위)를 초과하는 `wgetch()` 타건은 메모리에 담지 않고 무시하도록 블로킹
  - [x] **[UI 디테일]** 입력 윈도우 로드 시 우측 끝라인에 맞추어 `mvwprintw`를 사용해 '퀵 헬프(단축키 매뉴얼)' 사전 렌더링
  - [x] **[시스템 타임]** 채팅 로그 `wprintw()` 렌더링 시, 사전에 `<time.h>` 의 `localtime()`을 파싱하여 실시간 타임스탬프 규격 가공
  - [x] 수신 스레드에 의한 `wrefresh` 갱신 튕김 여파 방어용 `wmove()` 프롬프트 입력 커서 원위치 강제 복귀 루틴
  - [ ] **[3주차 최우선]** 수신 스레드에서 게임 패킷(`PKT_RES_BUY_OK`, `PKT_RES_SELL_OK`, `PKT_RES_DISPOSE_OK`, `PKT_RES_INVEN_INFO`, `PKT_RES_ERROR`) 파싱 및 UI 반영 + 입력 명령(`/buy` `/sell` `/dispose` `/inventory` `/rumor`) 파싱·전송 (B가 패킷 구조 확정 완료 — 단 현재 `ui_client.c`는 `PKT_EVT_CHAT`만 렌더링하고 입력도 전부 `PKT_REQ_CHAT`로만 전송. 이게 없으면 UI 클라로 게임 시연 불가, headless `client_main.c`엔 구현됨)
- [ ] **터미널 복구 복원 및 사보타주 연출 (Signals & Fail-Safe)**
  - [x] **[Fail-Safe 환경 복구]** 비정상 오류나 `SIGINT`로 프로그램 이탈 전, 반드시 `endwin()`을 호출하도록 `atexit` 또는 시그널 핸들러 상단에 방어 코드 결합 (터미널 오작동 원천 차단)
  - [ ] 사보타주 패킷 수신 시, 터미널 색상(`start_color`)과 반전(`A_REVERSE`)을 무작위로 뒤섞는 텍스트 글리치 랜덤 렌더링
  - [ ] 경찰 레이드 수신 중 오직 `Ctrl+C` 에 반응하는 시그널 하드웨어 후킹(`signal(SIGINT)`) 및 비상 탈출을 위한 `Ctrl+\` (`SIGQUIT`) 복구 종료 연동
  - [ ] `is_panic = 1;` 변수 감지 시 암호 타이핑 미니게임 렌더링 전환 (제한시간 오버 시 강제 서버 이탈 연동)
  - [ ] **[방탈출 성공 복구]** 서버로부터 `PKT_RES_MINIGAME_OK` 수신 시, 강제 잠금(Lock)을 해제하고 정상 마켓 화면으로 상태 머신(State Machine) 복귀
  - [ ] **[다중 태그 렌더링]** 기밀 문서가 가진 다중 태그(tags) 출력 시, 32비트 루프를 돌며 비트마스크(`& 1U << i`)로 분리해 `get_tag_name()`을 개별 호출하여 조합 출력
  - [ ] **[게임 오버 연출]** 경찰 습격 리셋 패킷 수신 시 **1분 소각 타이머** 및 최종 정산 랭킹 스코어보드를 렌더링하고, 에필로그 채팅을 활성화한 뒤 타이머 만료 시 클라이언트를 초기 상태로 리셋 또는 안전 정상 종료
- **분담(3주차):**
  - **B:** 유동적 목표 상환액 상태 관리, `/payoff` 성공 감지·자금 회수, 탈출 카운터 + 목표액 전역 인상/누적 3명 리셋 트리거 발송
  - **A:** 목표액 인상 브로드캐스트, 경찰 습격 1분 소각 리셋 시퀀스 브로드캐스트(별도 타이머 스레드)
  - **C:** `/payoff` 입력, 갱신된 목표액 표시, 경찰 습격 1분 타이머/스코어보드/에필로그 후 클라이언트 초기화 리셋
